#include <stdio.h>

int main(){ // addition de valeures
    
    int age = 12;
    float nom = 25.5;
    float somme = age + nom;

  
    printf("resulat = %d + %f = %f\n", age, nom, somme);

    return 0;
}